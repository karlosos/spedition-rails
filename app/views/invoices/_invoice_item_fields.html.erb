<li>
  <%= f.label "Destroy" %>
  <%= f.hidden_field :_destroy %>
  <%= link_to "remove", "#", class: "remove_invoice_items" %> <br>
  <%= f.label :quantity %>
  <%= f.number_field :quantity %> <br>
  <%= f.label :unit_price_cents %>
  <%= f.number_field :unit_price_cents %> <br>
  <%= f.collection_select :item_id, Item.all, :id, :name %>
  <!-- <%= f.fields_for :item do |item| %>
    <%= item.label :name %> <br>
    <%= item.text_field :name %> <br>
    <%= item.label :unit %> <br>
    <%= item.text_field :unit %>
  <% end %> -->
</li>
