<!-- <% if current_user.in_group?(@group, as: 'spedition') || current_user.in_group?(@group, as: 'admin') %>
<h3>Spedycja</h3>
<div class="row">
  <%= link_to raw(tile("Zlecenia")), speditor_view_path(:year => Date.today.year, :month => Date.today.month, :speditor_id => current_user.id) %>
  <%= link_to raw(tile("Nowe zlecenie")), new_transport_order_path %>
  <%= link_to raw(tile("Twoje samochody")), carrier_memberships_path %>
</div>
<% end %>

<% if current_user.in_group?(@group, as: 'accounting') || current_user.in_group?(@group, as: 'admin')%>
<h3>Księgowość</h3>
<div class="row">
  <%= link_to raw(tile("Faktury VAT")), invoices_path(:kind => 'vat') %>
  <%= link_to raw(tile("Zlecenia")), accounting_view_path %>
  <%= link_to raw(tile("Nowa faktura")), new_invoice_path %>
  <%= link_to raw(tile("Faktury proforma")), invoices_path(:kind => 'proforma') %>
  <%= link_to raw(tile("Faktury korekta")), invoices_path(:kind => 'correction')%>
</div>
<% end %>

<% if current_user.in_group?(@group) %>
<h3>Słowniki</h3>
<div class="row">
  <%= link_to raw(tile("Klienci")), clients_path %>
  <%= link_to raw(tile("Przewoźnicy")), carriers_path %>
  <%= link_to raw(tile("Towary")), items_path %>
</div>
<% end %>

<% if current_user.in_group?(@group, as: 'admin') %>
<h3>Administrator</h3>
<div class="row">
  <%= link_to raw(tile("Użytkownicy grupy")), @group %>
  <%= link_to raw(tile("Wartości domyślne")), edit_default_value_path(@group.default_value) %>
  <%= link_to raw(tile("Wszystkie zlecenia")), transport_orders_path %>
  <%= link_to raw(tile("Wszystkie faktury")), invoices_path %>
</div>
<% end %> -->

<form class="home-form" role="search">
  <div class="input-group">
      <input type="text" class="form-control" placeholder="Search" name="q">
      <div class="input-group-btn">
          <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
      </div>
  </div>
</form>
